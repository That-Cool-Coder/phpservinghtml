function squareNum(num) {
    return `${num} squared = the square root of ${num} (which is ${Math.sqrt(num)}) squared then squared again`;
}

function cubeNum(num) {
    return `${num} cubed = the square root of ${num} (which is ${Math.sqrt(num)}) squared then cubed`;
}